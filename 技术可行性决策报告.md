# 技术可行性决策报告 — KnowledgeHub v1.0 视频收藏导入

> **报告对象**：产品、研发、测试团队
> **决策日期**：2026-08-06
> **撰写人**：产品经理

## 1. 背景与目标
KnowledgeHub 核心价值之一是“把视频收藏变成可检索的知识”。针对平台 v1.0 上线目标，我们计划支持用户导入 **B站** 和 **抖音** 的收藏夹数据，由 AI Agent 提取标签与摘要。

## 2. 核心技术约束（不可抗力）
经过调研，B站和抖音平台在 API 开放能力上存在以下刚性约束：

1. **B站开放平台**：OAuth 2.0 授权 Scope **仅包含基础用户信息**（头像、昵称），**不包含**任何读取收藏夹内容的权限。
2. **抖音开放平台**：API 接口列表**完全不存在**与收藏夹相关的读取接口。
3. **用户隐私红线**：无法要求用户将平台 Cookie（SESSDATA）传给第三方服务端，这属于严重违规且极度不安全；用户也无法接受。

**结论**：**传统的“服务端通过官方接口抓取”方案彻底行不通。** 必须寻找基于客户端（浏览器端）的技术替代方案。

## 3. 候选方案与可行性评估
针对上述约束，团队对以下 4 个替代方向进行了技术验证与评估：

| 候选方案 | 技术实现逻辑 | 安全合规性 | 用户体验门槛 | 研发成本/复杂度 |
| :--- | :--- | :--- | :--- | :--- |
| **A. 用户上传文件** | 用户使用官方或第三方工具导出文件（.json/.csv），然后上传到平台解析。 | **极高**（数据不经手任何第三方服务器，无泄露风险） | 中等（用户需要3步：导出、上传、等待解析） | **极低**（仅需后端实现文件解析逻辑） |
| **B. 浏览器 Console 脚本** | 在平台页面提供一段原生 JS 代码，用户复制后在 B站 F12 控制台粘贴执行，自动抓取并 POST 给我们的后端。 | **高**（数据直接在用户本地浏览器由用户自己授权执行，无明文密码泄露） | 中等偏低（部分用户畏惧控制台，需图文引导） | **低**（纯前端 JS 代码，无需额外后端接口） |
| **C. 浏览器插件** | 开发 Chrome/Firefox 扩展，在 B站/抖音收藏页注入按钮，实现“一键提取”。 | **高**（同方案 B） | **极佳**（对用户无感知，点击即用） | **高**（需开发、上架扩展市场、维护多平台兼容） |
| **D. 浏览器书签脚本** | 制作一个 JS 代码片段压缩为书签，用户点击书签即可触发抓取。 | **高**（同方案 B） | 中等（需拖拽书签，配合多步图文教程） | **低**（纯代码无需发布更新） |

> *注：基于合规考虑，所有涉及用户隐私数据的动作，**必须发生在用户的浏览器终端**，而非由我们的服务端主动爬取。*

## 4. 决策矩阵与最终结论
为确保 **v1.0 按时上线**，同时预留未来的体验升级空间，我们采用 **“分阶段落地”** 策略。

**综合评分矩阵（满分 5 分）**：

| 指标 | 方案A（文件上传） | 方案B（Console脚本） | 方案C（浏览器插件） |
| :--- | :--- | :--- | :--- |
| 安全合规 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 技术可行性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| 用户操作门槛 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 开发与维护成本 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **推荐指数** | **⭐ ⭐ ⭐ ⭐ ⭐** | ⭐⭐⭐⭐ | ⭐⭐ |

**最终决策：**
> **v1.0 优先上线【方案 A：用户手动导出文件上传】。**
> **理由**：该方案在安全合规、技术可行性和开发成本三者之间取得了最优平衡。它能保证产品在没有任何后端风险的情况下，用最低的研发成本实现核心流程的闭环。

## 5. 分阶段实施路线图
我们选择**渐进式升级**：

| 版本 | 迭代目标 | 技术方案 | 关键成效 |
| :--- | :--- | :--- | :--- |
| **v1.0（当前版本）** | 快速跑通 MVP，验证价值 | **文件上传（.csv/.json）** | 建立核心认知：用户知道系统能分析视频并生成大纲。 |
| **v1.1（下个版本）** | 降低操作门槛，提升转化 | **内置 Console 脚本 + 友好图文引导** | 把“找第三方工具”变为“复制粘贴几行代码”，大幅提高使用率。 |
| **v2.0（远期规划）** | 极致体验，一键完成 | **Chrome 浏览器插件** | 收藏夹页面出现“一键导入”，体验对标 Notion Web Clipper。 |

## 6. 对产品体验的补偿设计
由于 v1.0 采用的是**文件上传**方案，用户必然会遇到“我怎么导出文件”的困惑。为优化体验，我们在前端做了以下补偿设计：

1. **保姆级分步引导**：在“视频收藏”面板，直接展示**3 步贴图教程**（如何用第三方工具导出的截图标注）。
2. **防误触提示**：在文件上传区明确标注“仅支持 .json 和 .csv 格式”，防止用户传错导致失败。
3. **Agent 解析反馈**：上传后立刻显示“Agent 正在分析数据...”，并通过进度条或状态卡展示解析进度（解析中/完成/失败），让用户对 AI 的“黑盒”操作有掌控感。

## 7. 总结
在资源有限、平台 API 全面受限的前提下，我们**不盲目追求一步到位的“完美体验”**，而是通过**“文件上传作为兜底方案”**和**“多阶段技术演进”**，保证了 v1.0 核心功能的按时交付，同时也为后续版本留下了清晰、可行的技术演进路径。
